# Dépendances de l'agent TrueSight (Windows, Python 3.12)
# ----------------------------------------------------------------------------
# Client HTTP sortant (poll vers le serveur), avec gestion fine des timeouts.
requests>=2.31,<3.0
# Métriques système : CPU, RAM, disques, uptime, sessions utilisateur.
psutil>=5.9,<6.0
# Accès aux API Windows : service Windows, DPAPI (win32crypt), registre, etc.
pywin32>=306
# Inventaire matériel via WMI (Win32_ComputerSystem, Win32_BIOS, ...).
WMI>=1.5.1
# ----------------------------------------------------------------------------
# Bureau à distance (module remote/) :
# Capture d'écran rapide (multi-moniteurs) — chemin ASSISTÉ (session utilisateur).
mss>=9.0,<10.0
# Capture DXGI Desktop Duplication — chemin NON-ASSISTÉ (helper SYSTEM). SEUL moyen
# de capturer le BUREAU SÉCURISÉ (écran de connexion / verrouillage / UAC) : GDI
# (mss) y renvoie une image NOIRE. Tire numpy + comtypes (voir capture_dxgi.py).
dxcam>=0.3,<0.4
# Encodage JPEG des trames (repli si PyTurboJPEG absent) + downscale.
pillow>=10.0,<12.0
# Client WebSocket synchrone (session wss vers le relais).
websocket-client>=1.7,<2.0
# Écoute audio distante : capture du son système (WASAPI loopback) via PortAudio.
# Embarque le module compilé _portaudiowpatch (PortAudio statique) — voir audio.py.
PyAudioWPatch>=0.2.12,<0.3
# ----------------------------------------------------------------------------
# Terminal interactif (module terminal/) :
# PTY ConPTY pour lancer un vrai shell PowerShell/cmd dont l'E/S transite par
# le même relais WebSocket (façon SSH-dans-le-navigateur).
pywinpty>=2.0,<3.0
# Encodage JPEG accéléré (libjpeg-turbo) : utilisé en priorité s'il trouve la DLL
# native ``turbojpeg.dll`` ; sinon repli automatique sur Pillow (cf. capture.py).
# Le build (build.ps1) embarque la DLL via --add-binary si elle est présente sur
# le poste de build (C:\libjpeg-turbo64\bin ou variable TURBOJPEG_DLL).
PyTurboJPEG>=1.7
numpy>=1.26  # requis par PyTurboJPEG (chemin TurboJPEG.encode).
# ----------------------------------------------------------------------------
# Outil de build (développement uniquement) : production de l'exécutable .exe.
pyinstaller>=6.3
